//kmk.kaushik
//ch.en.u4cys21027
#include <stdio.h>
int main( )
{

    // c question
    if ( 'X' < 'x' )
        printf ( "ascii value of X is smaller than that of x\n" ) ;

    // c answer
    // No error
}