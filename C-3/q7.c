//kmk.kaushik
//ch.en.u4cys21027
#include <stdio.h>
int main( )
{

    // g question
    
        int a, b ;
        scanf ( "%d %d",a, b ) ;
        if ( a > b ) ;
            printf ( "This is a game" ) ;
        else
            printf ( "You have to play it" ) ;
        return 0;
    
    // g answer
    // &a and &b should be used instead of a and b in scanf();
    
}