//kmk.kaushik
//ch.en.u4cys21027
#include <stdio.h>
int main( )
{

    // f question
    
    int x = 30, y = 40 ;
    if ( x == y )
    printf ( "x is equal to y\n" ) ;
    elseif ( x > y );
    printf ( "x is greater than y\n" ) ;
    elseif ( x < y );
    printf ( "x is less than y\n" ) ;
    return 0 ;
    
    // f answer
    // There should be a space between else and if, as there is no "elseif" syntax in C,"else if" exists.

    
}